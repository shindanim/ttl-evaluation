name: kg-eval
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - flask>=3.0
  - rdflib>=7.0
  - scikit-learn>=1.4
  - pytest>=8.0
  - pip
  - pip:
      - pyvis>=0.3.2
      - sentence-transformers>=2.7.0

